<li class="blog-tag tag_<%= tag.id %>">
  <span><%= tag.text %></span>
  <strong class="resource-action-divider has-text-link">|</strong>
  <%= link_to("Delete", tag_path(tag), method: :delete, remote: :true, class: "has-text-link", data: { confirm: "Are you sure you want to delete the '#{tag.text}' tag? This action is permanent." }) %>
</li>
